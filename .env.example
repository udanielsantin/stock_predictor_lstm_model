# AWS Configuration for Stock LSTM Predictor
# 
# ⚠️ IMPORTANTE: Este é um arquivo EXEMPLO
# Crie seu próprio .env com suas credenciais AWS
# NUNCA faça commit deste arquivo no git!

# AWS Access Key (obtém na AWS Console > IAM > Users > Access Keys)
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE

# AWS Secret Key (obtém na AWS Console > IAM > Users > Access Keys)
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY

# AWS Region (mude se seu bucket está em outra região)
AWS_REGION=us-east-1

# S3 Bucket para armazenar logs (não mude este valor)
S3_BUCKET_NAME=vapor-stock-predictor-logs

# Prefixo dos logs no S3 (não mude este valor)
S3_LOG_PREFIX=logs/

# ============================================================
# COMO CRIAR ESTE ARQUIVO NA SUA EC2:
# ============================================================
#
# 1. SSH para sua instância:
#    ssh -i seu-key.pem ubuntu@seu-ec2-ip
#
# 2. Navegue até o diretório do projeto:
#    cd stock_predictor_lstm_model
#
# 3. Crie o arquivo .env:
#    cat > .env << EOF
#    AWS_ACCESS_KEY_ID=sua-chave-aqui
#    AWS_SECRET_ACCESS_KEY=sua-senha-aqui
#    AWS_REGION=us-east-1
#    S3_BUCKET_NAME=vapor-stock-predictor-logs
#    S3_LOG_PREFIX=logs/
#    EOF
#
# 4. Altere as permissões:
#    chmod 600 .env
#
# 5. Verifique:
#    cat .env
#
# ============================================================
# SEGURANÇA:
# ============================================================
# - Este arquivo está no .gitignore e NÃO será commitado
# - Tenha cuidado ao compartilhar - contém credenciais sensíveis
# - Use credenciais com permissões mínimas (apenas S3)
# - Considere usar IAM roles no EC2 em vez de credenciais diretas
#
# ============================================================
